# Copy to .env and fill in. .env is gitignored. NEVER commit real values.

# Reddit OAuth app (https://www.reddit.com/prefs/apps) — "script" type.
# NOTE: the free Reddit API tier is non-commercial; a commercial product needs
# written approval + a paid contract. See CLAUDE.md / README.
REDDIT_CLIENT_ID=
REDDIT_CLIENT_SECRET=
# Only needed for "script" apps acting on a user account (optional for read-only):
REDDIT_USERNAME=
REDDIT_PASSWORD=

# Instagram (Instagram Login flow). Per-account token, keyed by uppercased
# account id: IGBOT_TOKEN_<ACCOUNT_ID>. Example for id "acct_main":
IGBOT_TOKEN_ACCT_MAIN=
